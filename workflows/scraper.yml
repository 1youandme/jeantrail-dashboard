name: 🔎 Scrape Alibaba Products

on:
  workflow_dispatch:       # يدوي
  schedule:
    - cron: '30 2 * * *'   # يوميًا الساعة 2:30 صباحًا UTC

jobs:
  scrape-products:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout Repo
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: 📦 Install Dependencies
        run: npm install puppeteer fs-extra

      - name: 🧠 Run Scraper
        run: node alibaba_scraper/scrape_products.js

      - name: 🔄 Commit scraped products
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add products/results/*.json
          git diff --cached --quiet || git commit -m "🆕 Auto update extracted products"
          git push
